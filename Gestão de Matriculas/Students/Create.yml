info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/students
  body:
    type: json
    data: |-
      {
        "name": "Fulana",
        "cpf": "000.000.000-05",
        "email": "email@email.com", // opcional
        "phone": "90000-0000" // opcional
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
