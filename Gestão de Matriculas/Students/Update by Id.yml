info:
  name: Update by Id
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/students/{{student_id}}
  body:
    type: json
    data: |-
      {
        "name": "Fulana updated", // opcional
        "cpf": "000.000.000-05", // opcional
        "email": "email@email.com", // opcional
        "phone": "90000-0000" // opcional
      }
  auth: inherit

runtime:
  variables:
    - name: student_id
      value: 78528cab-1e41-44d7-9cb3-fb4982f2ea35

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
